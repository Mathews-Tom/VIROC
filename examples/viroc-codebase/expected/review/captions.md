# Captions

- `0–120` From a topic to a verified, portable video.
- `120–270` Start from a concept: a repo, a document set, and a topic brief, not renderer code.
- `270–450` The guided planner derives a script, a scene plan, and an outline before any IR exists.
- `450–600` The approved outline becomes editable VidIR the user and agents refine.
- `600–780` viroc check surfaces typed VIR diagnostics so the storyboard is repaired before render.
- `780–960` The review surface shows scene cards and the script as an inspectable artifact before final render.
- `960–1140` One resolved Concrete IR compiles deterministically to Manim, HTML, and Remotion source.
- `1140–1320` Backend paths compared side by side; source hashes and build.json close the proof.
- `1320–1440` Reviewable, testable, reproducible — VidIR is the source of truth.
